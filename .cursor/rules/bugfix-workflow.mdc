---
description: 修复 Bug 的根因优先流程与验证规范
alwaysApply: true
---

# Bug 修复工作流

## 目标
- 修复 Bug 时优先定位根因。
- 处于 Bug 修复阶段时避免大范围重构。
- 每次改动保持最小、可验证、可回退。

## 必须流程
1. 先复现并记录第一个阻塞错误。
2. 区分根因与连锁/次生错误。
3. 先做能修复根因的最小改动。
4. 每次修复后先重新构建/测试，再进行下一处修改。
5. 以小步迭代方式持续推进，直到阻塞项清零。

## 优先级
- 编译/运行阻塞错误优先于 warning。
- 与修复无直接关系的清理项应延后处理。
- Bug 修复提交中不要混入功能开发改动。

## 验证标准
- 每次修复后先跑最小范围验证命令。
- 结束前必须跑一遍用户要求的目标流程（端到端）。
- 若未完全修复，必须明确给出下一个阻塞错误。

## 汇报格式
- 根因：哪里失败、为什么失败。
- 改动：改了什么、改在哪些位置。
- 验证：执行了什么命令、结果是什么。
- 下一阻塞：仅在未完全修复时给出。
